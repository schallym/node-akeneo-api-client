export type AkeneoAuthConnectionConfig = {
  baseUrl: string;
  clientId: string;
  secret: string;
  username: string;
  password: string;
};

export type AkeneoAuthAppConfig = {
  baseUrl: string;
  accessToken: string;
  clientId: string;
};
